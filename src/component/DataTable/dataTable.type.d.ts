type defaultProps = {
  row: GridRowsProp;
  columns: GridColDef[];
  options: {
    disableFilter?: boolean;
    disablePagination?: boolean;
    customFilter?: boolean;
    loading: boolean;
  };
};
